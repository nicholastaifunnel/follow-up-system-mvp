{
  "name": "follow-up-system-mvp",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "lint": "tsc --noEmit",
    "build": "tsc",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^6.8.2"
  },
  "devDependencies": {
    "prisma": "^6.8.2",
    "typescript": "^5.8.3"
  }
}
